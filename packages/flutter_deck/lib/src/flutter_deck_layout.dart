import 'package:flutter/widgets.dart';

/// A class that contains the layout constants for the slide deck.
class FlutterDeckLayout {
  /// The padding to use for the slide content.
  static const slidePadding = EdgeInsets.all(16);
}
